# Docs for the Azure Web Apps Deploy action: https://github.com/Azure/webapps-deploy
# More GitHub Actions for Azure: https://github.com/Azure/actions

name: Build and deploy container app to Azure Web App - agl-challenge

on:
  push:
    branches:
      - master
  workflow_dispatch:

jobs:
  build:
    runs-on: 'ubuntu-latest'

    steps:
    - uses: actions/checkout@v2

    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v1

    - name: Log in to registry
      uses: docker/login-action@v1
      with:
        registry: https://aglacr.azurecr.io/
        username: ${{ secrets.AzureAppService_ContainerUsername_11137258f6b143dabdf3faa2bd322eb9 }}
        password: ${{ secrets.AzureAppService_ContainerPassword_5743f6d6351c4219b86644834c33f482 }}

    - name: Build and push container image to registry
      uses: docker/build-push-action@v2
      with:
        push: true
        tags: aglacr.azurecr.io/${{ secrets.AzureAppService_ContainerUsername_11137258f6b143dabdf3faa2bd322eb9 }}/aglacr/eglmlchallenge:${{ github.sha }}
        file: ./Dockerfile

  deploy:
    runs-on: ubuntu-latest
    needs: build
    environment:
      name: 'production'
      url: ${{ steps.deploy-to-webapp.outputs.webapp-url }}

    steps:
    - name: Deploy to Azure Web App
      id: deploy-to-webapp
      uses: azure/webapps-deploy@v2
      with:
        app-name: 'agl-challenge'
        slot-name: 'production'
        publish-profile: ${{ secrets.AzureAppService_PublishProfile_7f538e74bcab4bf493b933e6d8643e36 }}
        images: 'aglacr.azurecr.io/${{ secrets.AzureAppService_ContainerUsername_11137258f6b143dabdf3faa2bd322eb9 }}/aglacr/eglmlchallenge:${{ github.sha }}'